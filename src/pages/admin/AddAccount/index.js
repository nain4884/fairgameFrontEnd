
import { Header, Background, HomeSlide, Account, AccountList, UserDetailModal, DepositModal, SetCreditModal, WithDrawModal, SetExposureModal, ChangePasswordModal, DailogModal, AddAccount } from "../../../components";

export default function AddAccountScreen() {

    return (
        <Background>
            {/* <Header /> */}
            <AddAccount />
        </Background>
    )
}
